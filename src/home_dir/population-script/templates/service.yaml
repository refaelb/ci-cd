apiVersion: v1
kind: Service
metadata:
  name: {{ include "population-script.fullname" . }}
  labels:
    {{- include "population-script.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "population-script.selectorLabels" . | nindent 4 }}
