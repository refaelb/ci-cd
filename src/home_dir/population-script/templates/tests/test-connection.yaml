apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "population-script.fullname" . }}-test-connection"
  labels:
    {{- include "population-script.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "population-script.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
