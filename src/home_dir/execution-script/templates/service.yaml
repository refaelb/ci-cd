apiVersion: v1
kind: Service
metadata:
  name: {{ include "execution-script.fullname" . }}
  labels:
    {{- include "execution-script.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "execution-script.selectorLabels" . | nindent 4 }}
