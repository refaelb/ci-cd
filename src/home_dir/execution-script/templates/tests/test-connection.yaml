apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "execution-script.fullname" . }}-test-connection"
  labels:
    {{- include "execution-script.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "execution-script.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
