apiVersion: v1
kind: Service
metadata:
  name: {{ include "bulk-service.fullname" . }}
  labels:
    {{- include "bulk-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bulk-service.selectorLabels" . | nindent 4 }}
