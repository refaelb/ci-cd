apiVersion: v1
kind: Service
metadata:
  name: {{ include "spike-service.fullname" . }}
  labels:
    {{- include "spike-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spike-service.selectorLabels" . | nindent 4 }}
