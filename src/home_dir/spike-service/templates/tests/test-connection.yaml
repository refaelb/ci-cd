apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spike-service.fullname" . }}-test-connection"
  labels:
    {{- include "spike-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spike-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
