apiVersion: v1
kind: Service
metadata:
  name: {{ include "approver-service.fullname" . }}
  labels:
    {{- include "approver-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "approver-service.selectorLabels" . | nindent 4 }}
