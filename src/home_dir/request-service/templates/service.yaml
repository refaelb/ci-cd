apiVersion: v1
kind: Service
metadata:
  name: {{ include "request-service.fullname" . }}
  labels:
    {{- include "request-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "request-service.selectorLabels" . | nindent 4 }}
