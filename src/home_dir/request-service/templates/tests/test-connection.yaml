apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "request-service.fullname" . }}-test-connection"
  labels:
    {{- include "request-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "request-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
